# Public evidence bundle

This directory is intentionally empty in source-only builds. Do not add sample,
fallback, zero-address, or locally generated evidence here.

After the real Base Sepolia lifecycle completes, copy the six validated artifacts
and generated `manifest.json` here. The public frontend verifies every SHA-256
digest before displaying public proof or enabling `releaseMilestone`.

